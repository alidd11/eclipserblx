

## Remove "All" Category from Quick Nav

Remove the `{ label: 'All', slug: '', icon: Gift }` entry from the `CATEGORIES` array in `src/components/landing/CategoryQuickNav.tsx`. Also clean up the unused `Gift` import from lucide-react.

Single file change, one line removal + import cleanup.

